<div class="max-w-2xl mx-auto mt-10 p-8 bg-white shadow-md rounded-lg">
  <h2 class="text-2xl font-bold mb-6 text-gray-800">プロフィール設定</h2>

  <%= form_with(model: @user, local: true) do |f| %>
    <div class="mb-4">
      <%= f.label :name, "名前", class: "block text-sm font-bold mb-2" %>
      <%= f.text_field :name, class: "w-full p-2 border rounded" %>
    </div>

    <div class="mb-4">
      <%= f.label :email, "メールアドレス", class: "block text-sm font-bold mb-2" %>
      <%= f.email_field :email, class: "w-full p-2 border rounded" %>
    </div>

    <div class="mt-8 p-4 bg-gray-50 rounded-lg">
      <h3 class="font-bold text-gray-700 mb-2">パスワードの変更</h3>
      <p class="text-xs text-gray-500 mb-4">※変更しない場合は空欄のままにしてください</p>

      <div class="mb-4">
        <%= f.label :password, "新しいパスワード", class: "block text-sm font-bold mb-2" %>
        <%= f.password_field :password, class: "w-full p-2 border rounded" %>
      </div>

      <div class="mb-4">
        <%= f.label :password_confirmation, "パスワード（確認用）", class: "block text-sm font-bold mb-2" %>
        <%= f.password_field :password_confirmation, class: "w-full p-2 border rounded" %>
      </div>
    </div>

    <div class="mt-8 flex justify-between items-center">
      <%= f.submit "設定を保存する", class: "bg-blue-600 text-white px-6 py-2 rounded-lg font-bold hover:bg-blue-700 cursor-pointer" %>
      <%= link_to "キャンセル", dashboards_show_path, class: "text-gray-500 hover:underline" %>
    </div>
  <% end %>

  <div class="mt-20 pt-8 border-t-2 border-red-100">
    <h3 class="text-red-600 font-bold mb-2">危険な操作</h3>
    <p class="text-sm text-gray-600 mb-4">アカウントを削除すると、これまでの全ての現場・メンバー・配置データが完全に消去されます。復元はできません。</p>
    <%= button_to "アカウントを完全に削除する", user_path(@user), method: :delete,
        data: { confirm: "本当に削除しますか？全てのデータが消去されます。" },
        class: "text-red-600 border border-red-600 px-4 py-2 rounded hover:bg-red-50 font-bold" %>
  </div>
</div>
